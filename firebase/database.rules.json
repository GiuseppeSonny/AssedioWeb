{
  "rules": {
    "rooms": {
      ".indexOn": ["status"],
      "$room_id": {
        ".read": "auth != null",
        "players": {
          "$uid": {
            ".write": "auth != null && auth.uid === $uid"
          }
        }
      }
    }
  }
}
